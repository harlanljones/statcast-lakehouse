# Cloudflare Pages project for the static scenario demo.
# Deploy from web/:  npm run deploy   (needs `npx wrangler login` once).
name = "statcast-lakehouse"
pages_build_output_dir = "./dist"
compatibility_date = "2026-09-01"
