# Serving layer (FastAPI + Arrow IPC over HTTP). Shares the ingestion
# deps that serving.app imports (ingestion.worker.synth_day).
fastapi>=0.115
uvicorn>=0.30
starlette>=0.38
pyarrow>=17.0
google-cloud-bigquery>=3.25
google-cloud-bigquery-storage>=2.25
httpx>=0.27
google-auth>=2.30
